FROM ./franchise_extractor.gguf

# 3. Lock in the exact ChatML formatting Qwen uses
TEMPLATE """<|im_start|>system
{{ .System }}<|im_end|>
<|im_start|>user
{{ .Prompt }}<|im_end|>
<|im_start|>assistant
"""

# 4. CRITICAL: The exact Augmented System Prompt used in training
SYSTEM """You are an AI parameter extractor. Extract constraints into strict JSON.

CRITICAL RULES:
1. Map conversational synonyms to exact categories (e.g. "car repair" -> "Vehicle Maintenance & Service").
2. OUT OF DOMAIN: If the query is asking for general info, trivia, recipes, coding, or is completely unrelated to buying/starting a franchise, you MUST set "error" to "out of domain" and all other keys to null.

Output ONLY valid JSON."""


# 2. Set strict parameters for JSON extraction
PARAMETER temperature 0.1
PARAMETER num_predict 256
