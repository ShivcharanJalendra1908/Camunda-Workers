# configs/config.dev.yaml
app:
  environment: development

camunda:
  oauth:
    enabled: true
    client_id: "worker-client" 
    client_secret: "worker-secret"
    token_url: "https://us-dev-api.lemici.com/realms/camunda-platform/protocol/openid-connect/token"  # ⚠️ CHANGE THIS WHEN DOMAIN IP CHANGES
    audience: "zeebe-api"

auth:
  keycloak:
    url: "https://us-dev-api.lemici.com"  # ⚠️ CHANGE THIS WHEN DOMAIN IP CHANGES
    realm: "camunda-platform"
    issuer: "https://us-dev-api.lemici.com/realms/camunda-platform"  # ⚠️ CHANGE THIS WHEN DOMAIN IP CHANGES
    clientId: "lemici-frontend"
    # redirectUrl: "http://localhost:3000/callback"
    redirectUrl: "https://d595hydlunw5u.cloudfront.net/callback"  # ⚠️ CHANGE THIS WHEN DOMAIN CHANGES
    publicBaseUrl: "https://us-dev-api.lemici.com"    # ⚠️ CHANGE THIS WHEN DOMAIN IP CHANGES
    # post_logout_redirect_uri: "http://localhost:3000/login"
    post_logout_redirect_uri: "https://d595hydlunw5u.cloudfront.net/login"  # ⚠️ CHANGE THIS WHEN DOMAIN CHANGES
    
  session:
    secure: false  # Allow HTTP in dev
    sameSite: "Lax"

logging:
  level: debug
  format: console

database:
  postgres:
    host: postgres
    port: 5432
    database: franchises
    user: postgres
    password: postgres
    max_connections: 20
    max_idle: 5
  elasticsearch:
    addresses: ["http://elasticsearch:9200"]
    username: ""
    password: ""
  redis:
    address: redis:6379

apis:
  genai:
    base_url: "http://localhost:8080"
  web_search:
    api_key: "dev-api-key"
    engine_id: "dev-engine-id"

integrations:
  smtp:
    host: "email-smtp.us-east-1.amazonaws.com"
    port: 587
    username: "AKIASS3JXDPV5KM5MXAV"
    password: "BDRIBvKIYD/LWgYq2agdZZpLmAtgCQEFfhv5capmbF9w"
    use_tls: true
    default_from: "noreply@lemici.com"